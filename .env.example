# VPS Panel Environment Configuration

# General
NODE_ENV=production

# Panel Domain Configuration
PANEL_DOMAIN=panel.agenzia.com
PREVIEW_DOMAIN=preview.agenzia.com

# Database (PostgreSQL)
POSTGRES_DB=vps_panel
POSTGRES_USER=panel_user
POSTGRES_PASSWORD=change-this-secure-password

# Redis
REDIS_PASSWORD=change-this-redis-password

# JWT Authentication
JWT_SECRET=change-this-to-a-very-long-random-secret-key-at-least-32-characters
JWT_EXPIRES_IN=7d

# Cloudflare (for wildcard SSL - optional)
CLOUDFLARE_EMAIL=your-email@example.com
CLOUDFLARE_API_KEY=your-cloudflare-api-key

# Hostinger API (optional)
HOSTINGER_API_KEY=your-hostinger-api-key
HOSTINGER_API_URL=https://api.hostinger.com/v1

# Backup Configuration
UPLOADS_DIR=/var/www/uploads
BACKUP_EXPIRY_HOURS=24

# Google Drive Integration (optional)
# Create a service account in Google Cloud Console and paste the JSON credentials here
# GOOGLE_DRIVE_CREDENTIALS={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# FileBrowser System (Admin Only)
FILEBROWSER_SYSTEM_USERNAME=admin
FILEBROWSER_SYSTEM_PASSWORD=change-this-strong-password

# SMTP Display Credentials (shown in frontend UI)
SMTP_DISPLAY_USER=noreply@yourdomain.com
SMTP_DISPLAY_PASSWORD=change-this-smtp-password

# Google Drive Backup Folder (optional, derived from PANEL_DOMAIN if not set)
# GDRIVE_BACKUP_FOLDER=VPS-panel-agenzia-com-backups
